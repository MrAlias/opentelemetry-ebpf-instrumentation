# ADR 001: Java remote-parent reverse transport

- Status: accepted for proof of concept
- Scope: inbound Java HTTP/1.1 remote-parent retrieval
- Related issues: `MrAlias/opentelemetry-ebpf-instrumentation#2` and native
  sub-issues `#9` through `#40`

## Context

OBI observes an upstream trace parent below TLS. An unmodified OpenTelemetry or
Splunk Java agent needs that parent synchronously, before its normal server
instrumenter starts a span. The Java process must not receive BPF map file
descriptors, bpffs access, BPF capabilities, or a path that uses
`bpf_probe_write_user`.

The reverse transport is only one part of correctness. Both transports look up
the same request-owned kernel state. If the current execution cannot be mapped
unambiguously to one generation, the result is a miss regardless of transport.

## Decision

Use optional `BPF_CGROUP_SETSOCKOPT` and `BPF_CGROUP_GETSOCKOPT` programs on
the actual application socket as the primary transport. Use a connected dummy
socket only to probe hook availability. Use a credential-checked Unix-domain-
socket request and response service owned by OBI as the fallback.

This intentionally supersedes issue #20's proposed cached dummy-socket data
path. A dummy socket can prove that the hooks are callable, but it does not own
the accepted connection. Take and discard therefore run on the backend's
actual accepted socket, allowing socket-local negotiation and the
kernel-derived tuple and network namespace to bind a claim to that connection.

The sockopt program lives in a separate BPF object. OBI probes program-type
support and loads/attaches that object independently, so an unsupported kernel
does not prevent OBI or Java TLS instrumentation from starting. The custom
level is `0x4f42`; `getsockopt` option `0x4a01` performs take and `0x4a02`
performs discard. `setsockopt` option `0x4a03` performs a non-consuming
capability negotiation with the exact process capability generated by OBI and
registered by the JVM. `setsockopt` option `0x4a04` acknowledges the data-stage
binding, and `getsockopt` option `0x4a05` performs the capability health
exchange used by the dummy probe. Negotiation is socket-local and becomes
usable only after the data hook acknowledges the kernel-derived tuple, network
namespace, and request generation. All unrelated levels and options retain
normal kernel behavior.

The primary program derives the current process, PID namespace, kernel thread,
mounted virtual thread, and generation-bound executor owner in the kernel. It
copies the fixed record through the sockopt context, not a user-memory helper.
Take and discard compete for one generation-specific claim-map entry shared by
both transports.

The fallback listens at `/var/run/obi/java-remote-parent.sock` by default with
mode `0660`, fixed framing, short read/write deadlines, bounded concurrent
handlers, and exact-path cleanup. Its real parent directory must be owned by
OBI, use the configured group, have mode `0750`, and not be a symlink. The Java
helper accepts only a socket at the configured path whose peer UID exactly
matches the OBI UID supplied at attach time. `SO_PEERCRED` is authoritative for
the client process at the server. `/proc` validation binds the requested
namespace TID to that process and derives the BPF map key. A version 2 request
also carries the unpredictable nonzero process capability generated by OBI at
attach time; the server compares it with both the exact authorization and the
JVM registration so PID reuse cannot authenticate a buffered request or stale
cache entry. The application never selects a PID. Successful thread lookups
are cached, but the process, thread, PID-namespace identity, authorization, and
registration are revalidated on every cache hit.

In `auto` mode, OBI attempts the sockopt object and hooks and also starts and
maintains the Unix service after the shared maps and data hook are ready. Its
`availability/valid` event reports an OBI-side ready or preferred transport; it
is not an acknowledgement of the Java helper's choice or proof that a request
used that transport. The helper probes `getsockopt` first on a connected probe
socket and, if that fails, probes Unix. Forced modes probe only the requested
transport. A separate fixed Java configuration snapshot exports the requested
and selected transport, attempted-probe mask, both probe outcomes, and final
status from one completed native attempt. Fixed disabled, unknown,
pre-registration failure, invocation failure, and version-mismatch sentinels
cover states without a completed native attempt. The snapshot does not prove
request use. When OBI's UID cannot be mapped into a target JVM user namespace,
attach rejects forced Unix and narrows `auto` to `getsockopt` for that JVM.

For `getsockopt`, each decrypted receive negotiates the actual application
socket before one take or discard. Unix retrieval instead sends one
credential-checked request containing the logical TID and process capability.
A normal miss never tries the other transport. Reconfiguration-class errors
fail the current request open. After the one-second backoff, a later request is
eligible to reprobe the originally requested mode; `auto` may then choose a
different transport. Transport-labelled operation evidence comes from OBI's
aggregate `take` and `discard` counters. Java diagnostics classify statuses but
export configuration selection separately from the monotonic counters, so
trace assertions remain necessary to prove request use.

## Alternatives

Evidence labels distinguish proof level from decision confidence:

- **retained**: sanitized runtime evidence committed in this repository;
- **focused**: source or focused tests, without a retained representative run;
- **analysis**: design analysis only;
- **proposal**: an external proposal that is not an accepted, implemented
  dependency;
- **untested**: no retained validation for the stated property.

The following two tables evaluate the same nine candidates against every
criterion from
[issue #12](https://github.com/MrAlias/opentelemetry-ebpf-instrumentation/issues/12).

| Candidate | Request correlation | Synchronous before span | Kernel, cgroup, and namespace portability | Dynamic helper and static extension | Evidence |
| --- | --- | --- | --- | --- | --- |
| Cgroup sockopt on accepted application socket | Socket-local negotiation plus kernel tuple, network namespace, execution, and generation | Yes; one take or discard | Requires cgroup v2 sockopt and SockOps support, netns cookies, and hierarchy-root attachment; broader matrix untested | Dynamic helper: attach and reconfiguration supported; static extension: configured at JVM start and transport-agnostic | **retained**: [forced primary run](../../examples/apache-java-https/evidence/otel-getsockopt-tls13-7482d908/README.md) |
| Cgroup getsockopt on a dummy socket | Current execution, without accepted-connection ownership | Mechanically yes, but cannot authorize the request connection | Same cgroup sockopt requirements as the primary | Dynamic helper: probe and reprobe supported; static extension: does not consume dummy-socket data | **focused**: [JNI probe tests](../../pkg/internal/java/agent/src/test/c/remote_parent_jni_test.c) |
| Credential-checked Unix socket | Peer process, validated logical TID, process capability, and generation | Yes; one deadline-bounded RPC | Shares the producer's cgroup v2 SockOps and netns-cookie requirements; additionally requires a shared Unix endpoint and readable peer `/proc` identity across namespaces | Dynamic helper: attach, reconnect, and reconfiguration supported; static extension: configured at JVM start and transport-agnostic | **retained**: [forced Unix run](../../examples/apache-java-https/evidence/otel-unix-tls12-acedb68a/README.md) |
| Brokered BPF map FD | Application chooses a map key; no socket-local request authority | Yes | Requires transferable map FDs and stable namespace/lifetime handling | Dynamic helper: must receive and revoke the FD; static extension: bridge API could remain, but the helper couples to map ABI | **analysis**, **untested** |
| Direct bpffs lookup | Application chooses a map key; no socket-local request authority | Yes | Requires a bpffs mount and BPF permissions in the application environment | Dynamic helper: must discover and open pins; static extension: bridge API could remain, but the helper couples to pinned ABI | **analysis**, **untested** |
| Shared memory | Needs a second secure request-ownership protocol | Yes | Needs a safely shared mapping across the deployment's namespaces | Dynamic helper: must establish and remap it; static extension: remains isolated only if the bridge hides the mapping protocol | **analysis**, **untested** |
| Ring or perf buffer | Asynchronous event still needs request matching | No; delivery can race extraction | Requires direct BPF-buffer access or another broker channel | Dynamic helper: must consume or broker events; static extension: receives no guaranteed synchronous value | **analysis**, **untested** |
| Custom ioctl plus user-memory write | Could use current execution, but output authority is unsafe | Potentially | Requires a custom hook and unsafe user-memory output path | Dynamic helper: must install the native ABI; static extension: bridge API could remain, but the carrier remains unsafe | **analysis**, **untested** |
| Standard Process Context | Process and instrumentation identity only; no request value | No per-request value | Depends on future SDK adoption, not cgroup placement | Dynamic helper: could read discovery data; static extension: receives no parent; SDK or agent: must publish Process Context | **proposal**: [PR 5116](https://github.com/open-telemetry/opentelemetry-specification/pull/5116) |

| Candidate | Java privilege and attack surface | Restart and version skew | Latency, allocation, and backpressure | Maintenance and diagnostics | `bpf_probe_write_user` | Decision |
| --- | --- | --- | --- | --- | --- | --- |
| Cgroup sockopt on accepted application socket | Ordinary socket syscalls; no BPF access | Optional object, explicit ABI, socket renegotiation, and request-triggered reprobe after backoff | Latency: synchronous syscall, untested; allocation: reused Java buffers focused, retained allocation untested; backpressure: 64-slot provider pool returns overload, syscall has no configured deadline | Maintenance: separate BPF/JNI ABI; diagnostics: reason-coded OBI counters and Java status counters | No | Primary |
| Cgroup getsockopt on a dummy socket | Ordinary probe socket; no BPF access | Recreated by a request-triggered reprobe after backoff | Latency and allocation: socket-pair setup plus negotiate/health untested; backpressure: not applicable to the data path | Maintenance: small native probe surface; diagnostics: selected transport and both probe outcomes in one fixed Java result | No | Probe only |
| Credential-checked Unix socket | Access to one endpoint; peer UID remains in the trust boundary | Reconnectable, versioned framing, and request-triggered reprobe after backoff | Latency and allocation: comparative evidence untested; backpressure: absolute RPC deadline, bounded server admission, and 64-slot provider pool | Maintenance: server, framing, `/proc`, credentials, and path handling; diagnostics: reason-coded OBI counters plus fixed Java selection and probe outcomes | No | Fallback |
| Brokered BPF map FD | Grants a BPF map capability to the application | Revocation and map-version skew are difficult | Latency, allocation, and backpressure: all untested | Maintenance: FD broker plus internal map ABI; diagnostics: new broker and access-status reporting required | No | Reject |
| Direct bpffs lookup | Grants bpffs and BPF permissions | Stale pins and mixed map versions need cleanup | Latency, allocation, and backpressure: all untested | Maintenance: mounts, permissions, pins, and internal map ABI; diagnostics: new access and stale-pin reporting required | No | Reject |
| Shared memory | Requires a cross-process shared mapping; Java write authority depends on the design | Stale mappings and restart ownership are complex | Latency, allocation, and backpressure: all untested | Maintenance: ownership, version, and corruption protocol; diagnostics: new integrity and ownership reporting required | No | Reject |
| Ring or perf buffer | Requires map FD/BPF access, or another broker channel | Buffered events can be lost across restart | Latency and allocation: untested; backpressure: finite buffers can drop or back up before extraction | Maintenance: broker plus second correlation protocol; diagnostics: drop, lag, and unmatched-event reporting required | No | Reject alone |
| Custom ioctl plus user-memory write | Ordinary syscall with an unsafe output mechanism | Bespoke ABI and compatibility gates | Latency, allocation, and backpressure: all untested; verifier and memory-write failures add risk | Maintenance: bespoke kernel/native ABI; diagnostics: new verifier and memory-write status reporting required | Yes for the proposed user-write design | Reject |
| Standard Process Context | SDK-published process metadata can expose stack identity | Proposal defines process-level publication and versioning | Per-request latency, allocation, and backpressure: not applicable because it is not a carrier; publication costs untested | Maintenance: external specification, SDK adoption, and OBI scope mapping; diagnostics: process-level discovery only | No | Reject as carrier; useful for discovery |

Asynchronous channels cannot guarantee that a record is available before the
server propagator runs. At this ADR checkpoint, OpenTelemetry specification PR
5116 is an unaccepted proposal; this evaluation pins commit
[`8bdbb908`](https://github.com/open-telemetry/opentelemetry-specification/blob/8bdbb908b87f76da7439b33d16e0fa41a99c77fd/oteps/5116-process-ctx-instrumentations.md).
It extends Process Context with registered instrumentation libraries for
external readers; it does not publish a per-request trace ID or span ID and
therefore cannot carry this remote parent.

The retained runs establish the listed acceptance assertions and bounded
failure cases for one forced primary cell and one forced fallback cell. They do
not contain a representative primary-versus-fallback latency or allocation
comparison. The
[benchmark matrix](../../examples/apache-java-https/BENCHMARK.md) remains
**untested**, so this PoC decision is not a performance or production-support
commitment.

## Feature and fallback gates

Both transports first require compatible shared maps and an authoritative
producer/data hook. Sockopt availability in OBI additionally requires the first
three gates below; JVM selection requires the final gate:

- `CGroupSockopt` program-type probing succeeds at runtime;
- the separate object loads and its internal pinned maps match the producer;
- OBI attaches both programs at the resolved cgroup-v2 hierarchy root;
- the Java helper completes authenticated capability negotiation through the
  set-sockopt hook and receives the same capability from the get-sockopt
  health option on the connected probe socket.

The valid, missing, stale, and one-shot request cases are privileged acceptance
tests, not startup feature probes. Likewise, nested or competing cgroup
programs are deployment compatibility cases: the health exchange detects a
hook that blocks or rewrites the probe, but it cannot prove that no program at
another cgroup will affect a later application socket. Such topologies must be
tested explicitly and can select forced Unix mode when they are incompatible.

A primary-specific probe, load, attach, or helper health failure can leave Unix
available in `auto` mode when the bridge-wide prerequisites, shared maps, data
hook, and Unix listener are ready. A bridge-wide prerequisite failure leaves
both transports unavailable. Forced `getsockopt` reports failure instead of
silently falling back. Forced `unix` does not load or attach the sockopt object.
`disabled` leaves existing OBI and Java-agent behavior unchanged.

The fallback requires a shared filesystem location, peer credentials, readable
peer `/proc` identity, and strict socket ownership/mode. Endpoint replacement,
malformed framing, unauthorized identity, overload, timeout, and restart all
return a non-valid status and fail open.

TCP senders retain the 26-byte legacy option while this bridge is disabled.
When enabled, they send the 27-byte version that adds exact trace flags. New
receivers accept both sizes, but only exact-flag candidates may enter the Java
bridge. A rolling deployment must therefore upgrade all receivers while the
bridge remains disabled, then enable it. Rollback disables the bridge before
downgrading receivers. Sending the 27-byte option to an old receiver is outside
the supported mixed-version sequence and can break trace-context propagation.

## Security and limitations

The option pair is collision-resistant, not globally reserved. A program at a
nested cgroup can observe or override the same call. Runtime validation and the
forced fallback provide a diagnostic path, but deployments must attach OBI at
the hierarchy root resolved by OBI. Deploy one bridge owner per effective
cgroup-v2 hierarchy, or force Unix when an ancestor or descendant program is
incompatible.

The Unix socket authenticates a process, while request ownership is still
proved by the staged logical execution identity. The configured group grants
connect access, not permission to replace the endpoint. Deployments with
different application and OBI groups must create the shared directory with the
configured GID before startup; the demo uses an explicitly initialized named
volume. A compromised instrumented JVM may consume its own record. A process
running as the OBI UID can impersonate this local fallback, so the OBI UID and
host root remain inside the trust boundary. The endpoint and diagnostics do
not reveal context identifiers.

Fallback connections have one absolute deadline, a shorter pre-authentication
read deadline, a per-peer cap, and a global cap shared by pre-authentication and
authenticated handling. Saturation returns `overload` without looking up or
consuming context. Multiple cooperating processes that already have connect
permission can still exhaust the bounded admission pool and temporarily deny
fallback service; group membership is therefore part of the availability trust
boundary. The primary transport and application request processing remain
fail-open, and this local denial-of-service risk is accepted for the PoC.

Connection-only correlation is insufficient for pipelined, coalesced, or
multiplexed requests. Ambiguous HTTP/1.1 inputs deliberately miss. HTTP/2 is
unsupported until a stream-safe identity exists.

Each accepted socket descriptor is stored in a fresh, one-shot Java holder.
Exact executor, ForkJoin, Netty, and virtual-thread task captures share that
holder with the generation-bound task token. Task entry masks any prior worker
binding, task exit restores it, and take or discard atomically consumes the
shared descriptor before transport readiness, buffer acquisition, or native
lookup. Cancellation and rejection detach the task without invalidating a
holder that another exact alias may still claim. This supports instrumented
task ownership across a TLS-receive-to-extraction thread change without
duplicating or retaining the application socket. Packaged-agent probes prove
the task-scope mechanism; a framework-specific pre-extraction claim still
requires retained privileged trace evidence.

The descriptor is only a lookup handle. Socket-local negotiation and the exact
task generation still authorize retrieval, so close or numeric descriptor
reuse can cause a miss but cannot select a newer connection's parent. A
framework handoff outside the instrumented task surfaces remains an explicit
primary-transport miss and never falls back to a dummy socket or a
connection-only guess.

## Rollout and evidence

The default is `disabled`. The proof-of-concept runner can exercise
`getsockopt`, `unix`, `auto`, and disabled or absence controls. The
[retained evidence index](../../examples/apache-java-https/evidence/README.md)
currently contains one forced `getsockopt` full run and one forced Unix full
run. Disabled and absence behavior is retained only as controls within those
runs; no `auto` acceptance run is retained. Raw results are written under
`examples/apache-java-https/.runtime/results/`; sanitized durable artifacts
live under `examples/apache-java-https/evidence/`. Unexecuted cells remain
`untested` and are never inferred from kernel version strings.

Rollback sets the transport to `disabled` and removes `obi` from the ordered
Java propagator list. Neither action requires a custom Java-agent build.
